#ifndef LEB128_HPP
#define LEB128_HPP

namespace leb128 {
  std::uint8_t varuint8(wasm_stream &ws) {
  }
  std::uint32_t varuint32(wasm_stream &ws) {
  }
  std::uint64_t varuint64(wasm_stream &ws) {
  }
}

#endif

